const config = {
  thriftily: {
    app: true,
    agent: false,
    async: false,
    reconnect: true,
    maxAttempts: 0,
    attemptTime: 2000,
    clients: {},
    default: {}
  }
}

module.exports = config
